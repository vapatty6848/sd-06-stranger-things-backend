apps:

- name: ST-BE
  script: ./index.js
  exec_mode: cluster
  instances: 2
  max_memory_restart: 200M
  env:
    PORT: 3000
    UPSIDEDOWN_MODE: true
    GITHUB_USER: marianoelsztain
    NODE_ENV: development